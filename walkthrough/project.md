## Project editing

The **M1 Project** explorer view lists every group/channel with type, unit and security; right-click to edit. All edits go through `m1-project` (validated, atomic, encoding-safe) and hot-reload the language server.
